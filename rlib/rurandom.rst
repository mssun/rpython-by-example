rurandom
========

``rurandom`` uses system random number generator, which is suitable for
cryptographic use. In UNIX system, it read bytes from ``/dev/urandom``.

.. literalinclude:: ../code/rurandom.py
