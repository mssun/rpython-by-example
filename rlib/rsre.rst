rsre
====

``rsre`` provides regular expression matching operations same as the Python
``re`` module. You can do ``match()``, ``search()``, ``findall()``, ``finditer()``,
``split()``.

.. literalinclude:: ../code/rsre.py

More examples.

.. literalinclude:: ../code/rsre_more.py
