Foreign Function Interface (rffi)
=================================

The foreign function interface of RPython is called rffi.

* declaring low-level external function
* types
* registering function as external

.. literalinclude:: ../code/rffi_abs.py
